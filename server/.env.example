PORT=3000
JWT_SECRET=cambiar-por-un-valor-largo-y-aleatorio

# En production la cookie pasa a secure:true y sameSite:'none'.
NODE_ENV=development
CLIENT_URL=http://localhost:5173

# Vacío = usa SQLite local (data.sqlite). Con una URL de Postgres, usa Postgres.
DATABASE_URL=
DATABASE_SSL=

ADMIN_USERNAME=
ADMIN_PASSWORD=

# Variables usadas actualmente por src/config/database.js (MySQL)
DB_USER=
DB_PASSWORD=
DB_HOST=localhost
DB_NAME=
